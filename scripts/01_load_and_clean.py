# Load dataset
# Standardize column names
# Normalize gender values (map 1, M → 'M'; 2, F → 'F'; U stays)
# Handle missing values (e.g. fill with 'Unknown' or drop if necessary)
# Save cleaned version to outputs/
